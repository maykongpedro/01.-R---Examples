# mensagem de boas vindas
message("Hi, user! Welcome again!")

# substituir o print no console
paint::mask_print()
options(
  # remover o aviso
  paint_remind_mask_print = FALSE,
  # paleta de cores do paint
  paint_palette = paint::brewer_set3_12()
)

# indicando o uso do pacote httpgd ára gráficos
options(vsc.use_httpgd = TRUE)